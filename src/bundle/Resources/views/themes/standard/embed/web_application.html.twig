<iframe
        src="{{ web_application_url }}"
        id="web_application_{{ content.id }}"
        class="web_application web_application_embedded_by_{{ embedded_by|default('none') }}"
        width="100%" height="100%"
>
    {# TODO #}
</iframe>
